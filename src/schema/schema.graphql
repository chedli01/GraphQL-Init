type User {
    id:ID!
    name:String!
    email:String!

}
input UserInput {
  id: ID!
  name: String!
}

type Query {
  hello(user: UserInput): String!
  hi: String!
  getUsers : [User]!
}

type Mutation{
    addUser(id:ID,name:String,email:String):User!
}
